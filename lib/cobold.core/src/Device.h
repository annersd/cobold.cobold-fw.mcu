#pragma once

namespace cobold
{
    /**
     * @brief A cobold device represents a physical device that is connected to the cobold network.
     * 
     * @details A device contains a set of (hardware) components that are connected to the device.
     * 
     * @todo Implement this class.
    */
    class Device
    {
    private:
        /* data */
    public:
        Device(/* args */){};
        ~Device() = default;
    };
}